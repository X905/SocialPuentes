@model IEnumerable<SocialPuentes.Models.Sugerencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sugerencia1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Full_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sugerencia1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Full_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Sugerencias }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Sugerencias }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Sugerencias })
        </td>
    </tr>
}

</table>
