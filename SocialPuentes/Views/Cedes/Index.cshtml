@model IEnumerable<SocialPuentes.Models.Cede>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CedeNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ubicacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CedeNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ubicacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Cede }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Cede }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Cede })
        </td>
    </tr>
}

</table>
